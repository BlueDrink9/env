bash
neovim
syncthing
git
bash-completion@2
bash-completion # Only if 2 not a thing (ie not homebrewing).
tmux
ctags
python
curl
rsync
ranger
ripgrep
rlwrap
ssh-agent
ssh-pass
workrave

# proselint # wooah cool. Pip module

# OSX only
# Needs xcode-8 cmd tools
# brew install koekeishiya/formulae/skhd
# brew services start skhd
# while read -r pkg; do
    # pack install $pkg
# done < "$($SCRIPTDIR_CMD)/packages"
