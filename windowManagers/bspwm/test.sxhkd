# vim:ft=sxhkdrc
# vim:ft=sh
#
#
# bspwm hotkeys
#
# super + g
#   bspc config focused_border_color "#ffbc00"; sleep 1; \
#     bspc config focused_border_color "#00bcff"
# # super + w: super + w
# Define the mode. Mode actually sends a key combo we will never otherwise use!
# super + w: {a,_}
#   {bspc config focused_border_color "#00bcff";, echo ha}
  # bspc config focused_border_color "#00bc00" && xdotool key --clearmodifiers ctrl+super+F12
# usually works
super + w; x
  echo xtime
# usually works
super+w; z
  echo zzzlol
super + w
  echo nice; bspc config focused_border_color "$BSPWM_FOCUSED_COLOUR"

# Insert: super + g
#   bspc config focused_border_color "#247dcc"; xdotool key Escape
# Works
ctrl+super+F12
  echo nice
# doesn't
ctrl+super+F12; a
  echo hi
  # bspc config focused_border_color "#247dcc"
#worsk
ctrl+u; a
  echo hfi

# super + shift + Insert: super + g
#   bspc config focused_border_color "#247dcc"; xdotool key Escape
